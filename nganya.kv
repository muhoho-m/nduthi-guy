MainScreen:

<MainScreen>:
    canvas:
        Rectangle:
            pos: self.center_x-s/2, self.center_y-s/2
            size: s, s
        Ellipse:
            pos: 100, 200
            size: s, s/2
        Line:
            points: (0, 0, 100, 100, 200, 0, self.width, 200)